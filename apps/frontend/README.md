# Medora Frontend

React + Vite + Tailwind CSS dashboard for patients and doctors.

## Setup

```bash
npm install
cp .env.example .env   # fill VITE_API_BASE_URL
npm run dev
```

Runs at `http://localhost:5173`
